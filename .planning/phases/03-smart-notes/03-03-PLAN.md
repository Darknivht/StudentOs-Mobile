---
phase: 03-smart-notes
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/screens/notes/NoteViewerScreen.tsx
  - src/screens/notes/QuickActionsSheet.tsx
  - src/screens/notes/index.ts
  - src/screens/notes/NotesListScreen.tsx
autonomous: false
requirements:
  - NOTES-08
  - NOTES-12
must_haves:
  truths:
    - "User can view full note content in a dedicated viewer screen"
    - "User can access quick actions (view, summary, flashcards, quiz, delete) from note cards"
    - "Quick actions bottom sheet appears when long-pressing a note card"
    - "Delete action shows confirmation before removing note"
    - "Generate Flashcards and Generate Quiz actions navigate to placeholder screens"
  artifacts:
    - path: "src/screens/notes/NoteViewerScreen.tsx"
      provides: "Full note content viewer with HTML rendering"
      min_lines: 60
    - path: "src/screens/notes/QuickActionsSheet.tsx"
      provides: "Quick actions bottom sheet per D-03"
      min_lines: 50
  key_links:
    - from: "src/screens/notes/NotesListScreen.tsx"
      to: "src/screens/notes/NoteViewerScreen.tsx"
      via: "Navigation on note card press"
      pattern: "navigate.*NoteViewer"
    - from: "src/screens/notes/NotesListScreen.tsx"
      to: "src/screens/notes/QuickActionsSheet.tsx"
      via: "Long-press on note card opens actions sheet"
      pattern: "QuickActionsSheet"
    - from: "src/screens/notes/QuickActionsSheet.tsx"
      to: "src/screens/notes/NoteViewerScreen.tsx"
      via: "View action navigates to viewer"
      pattern: "navigate.*NoteViewer"
---

<objective>
Build the note viewer screen for full content display and the quick actions bottom sheet for note operations (view, generate summary, generate flashcards, generate quiz, delete).

Purpose: NOTES-08 requires a note viewer dialog with full text display, and NOTES-12 requires quick actions accessible from note cards. The quick actions sheet uses the @gorhom/bottom-sheet pattern established in D-03, and provides entry points for AI features (implemented in Plan 04) and flashcard/quiz features (implemented in future phases).

Output: Note viewer screen rendering full HTML content, quick actions bottom sheet triggered by long-press on note cards, with navigation to viewer, AI summary trigger, flashcard/quiz placeholders, and delete with confirmation.
</objective>

<execution_context>
@C:/Users/Toshiba/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Toshiba/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-smart-notes/03-CONTEXT.md
@.planning/phases/03-smart-notes/03-01-SUMMARY.md
@src/screens/notes/NotesListScreen.tsx
@src/screens/notes/NoteCard.tsx
@src/hooks/useNotes.ts
@src/lib/theme.ts
@src/types/note.ts
@src/navigation/types.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
From src/types/note.ts (created in Plan 01):
```typescript
export type SourceType = 'manual' | 'pdf' | 'docx' | 'image';
export interface NoteItem {
  id: string; userId: string; courseId: string | null;
  title: string; content: string; summary: string | null;
  sourceType: SourceType; fileUrl: string | null;
  originalFilename: string | null; isPinned: boolean;
  createdAt: string; updatedAt: string;
}
export interface NoteWithCourse extends NoteItem {
  courseName: string | null; courseEmoji: string | null; courseColor: string | null;
}
```

From src/hooks/useNotes.ts (created in Plan 01):

```typescript
export function useNotes(): {
  notes: NoteWithCourse[]; isLoading: boolean; refetch: () => void;
  createNote: (params: Partial(NoteItem)) => Promise(NoteItem);
  deleteNote: (id: string) => Promise(void);
  updateNote: (id: string, params: Partial(NoteItem)) => Promise(NoteItem);
  togglePin: (id: string, isPinned: boolean) => Promise(void);
};
```

From src/navigation/types.ts (updated in Plan 01):

```typescript
export type NotesStackParamList = {
  NotesMain: undefined;
  NoteEditor: { noteId?: string; courseId?: string };
  NoteViewer: { noteId: string };
};
```

From src/lib/theme.ts:

```typescript
export const colors = {
  background: "#0f0f23",
  foreground: "#ffffff",
  primary: "#7c3aed",
  card: "#12122a",
  border: "#2a2a4a",
  muted: "#1a1a2e",
  mutedForeground: "#6b6b8a",
  destructive: "#ef4444",
  success: "#22c55e",
  warning: "#f59e0b",
};
```

</interfaces>

<tasks>

<task type="auto">
<name>Task 1: Note viewer screen with full HTML content display</name>
<files>src/screens/notes/NoteViewerScreen.tsx, src/screens/notes/index.ts</files>

<read_first>

- src/screens/notes/NotesListScreen.tsx - Pattern reference for screen layout with safe area insets
- src/hooks/useNotes.ts - Data hook for fetching single note by ID
- src/lib/theme.ts - Design tokens
- src/types/note.ts - NoteWithCourse type
- src/navigation/types.ts - NoteViewer route params
- src/screens/notes/index.ts - Current exports to add NoteViewerScreen
  </read_first>

<action>
1. Install @gorhom/bottom-sheet (needed for this plan and Plan 04):
```bash
npx expo install @gorhom/bottom-sheet react-native-reanimated react-native-gesture-handler
```
@gorhom/bottom-sheet requires react-native-reanimated and react-native-gesture-handler as peer dependencies. These may already be installed; if so, expo install will skip them.

2. Create src/screens/notes/NoteViewerScreen.tsx:

- Receive noteId from route.params
- Fetch note data using useNotes hook - find note by ID from the notes array
- Layout (top to bottom):
  a. Top bar: back button (arrow left), note title (truncated), edit button (pencil icon)
  - Back button: navigation.goBack()
  - Edit button: navigation.navigate('NoteEditor', {noteId: note.id})
    b. Note metadata row: source badge icon + label, course tag pill (if courseId), date
    c. Note content: render full HTML content
  - Use WebView with inline HTML for rich text rendering (react-native-pell-rich-text's RichEditor in readOnly mode works too, but WebView is more reliable for full HTML display)
  - Alternative: use RichEditor from react-native-pell-rich-text with initialContentHTML and useEditor=false (read-only display)
  - Apply dark theme styling: inject CSS for body background #0f0f23, text color #ffffff, link color #7c3aed
    d. AI action FAB button at bottom-right (purple circular, sparkle icon)
  - onPress: open QuickActionsSheet (implemented in Task 2)
  - This FAB is per D-03 - floating action button at bottom-right of note viewer
- Use useSafeAreaInsets() for top/bottom padding (NOT SafeAreaView)
- Loading state: ActivityIndicator with colors.primary
- Error state: "Note not found" message with goBack button
- If note has summary: show summary section above content with "AI Summary" header and expandable/collapsible text
- HTML sanitization on display: use a simple regex to strip script tags and on\* attributes from content before rendering
  - content.replace(/<script[^>]_>[\s\S]_?<\/script>/gi, '').replace(/ on\w+="[^"]\*"/g, '')

3. Update src/screens/notes/index.ts to export NoteViewerScreen
   </action>

<verify>
<automated>npx tsc --noEmit --pretty 2>&1 | grep -E "NoteViewerScreen" | head -20; echo "EXIT:$?"</automated>
</verify>

<done>
- NoteViewerScreen renders full note content with HTML display
- Top bar has back button, note title, and edit button
- Note metadata shows source badge, course tag, and date
- AI action FAB appears at bottom-right of viewer
- HTML content is sanitized before rendering (script tags and on* attributes stripped)
- Summary section shown if note has AI-generated summary
- Edit button navigates to NoteEditor with noteId
- Loading and error states handled
- TypeScript compiles without errors related to these files
</done>
</task>

<task type="auto">
<name>Task 2: Quick actions bottom sheet and long-press trigger on note cards</name>
<files>src/screens/notes/QuickActionsSheet.tsx, src/screens/notes/NotesListScreen.tsx, src/screens/notes/NoteCard.tsx</files>

<read_first>

- src/screens/notes/NotesListScreen.tsx - Main list screen, needs long-press handler
- src/screens/notes/NoteCard.tsx - Card component, needs onLongPress prop
- src/hooks/useNotes.ts - deleteNote function
- src/lib/theme.ts - Design tokens
- src/types/note.ts - NoteWithCourse type
  </read_first>

<action>
1. Create src/screens/notes/QuickActionsSheet.tsx per D-03 (bottom sheet pattern):
- Use @gorhom/bottom-sheet BottomSheet and BottomSheetView components
- Wrap the app with BottomSheetModalProvider at the NotesListScreen level (required by @gorhom/bottom-sheet)
- Props: note: NoteWithCourse | null, sheetRef: React.RefObject of BottomSheet
- Sheet configuration: snapPoints: ['50%'], enablePanDownToClose: true, backgroundStyle: {backgroundColor: colors.card}
- Handle bar at top (default BottomSheet handle component)
- Title: "Quick Actions" in typography.xl, fontWeight '600', colors.foreground
- Action rows (each is a Pressable row with icon + title + description):
  a. View Note - icon: eye, title: "View Note", description: "Read the full note" - onPress: navigate to NoteViewer
  b. Generate Summary - icon: sparkle, title: "Generate Summary", description: "AI creates a summary of this note" - onPress: navigate to NoteViewer with a query param or callback to trigger summary (actual AI generation is Plan 04)
  c. Generate Flashcards - icon: cards, title: "Generate Flashcards", description: "Create flashcard deck from this note" - onPress: show Alert "Flashcards coming soon! This feature will be available in a future update." (Phase 5)
  d. Generate Quiz - icon: question, title: "Generate Quiz", description: "Create a quiz from this note" - onPress: show Alert "Quizzes coming soon! This feature will be available in a future update." (Phase 6)
  e. Delete Note - icon: trash, title: "Delete Note", description: "Permanently remove this note", destructive: true - onPress: show confirmation Alert.alert("Delete Note?", "This action cannot be undone.", [{text: "Cancel", style: "cancel"}, {text: "Delete", style: "destructive", onPress: () => deleteNote(note.id)}])
- Each row style: flexDirection 'row', alignItems 'center', padding spacing.lg, borderBottomWidth 1, borderBottomColor colors.border
- Icon container: 44x44, borderRadius 12, backgroundColor colors.muted, centered icon
- Destructive row: icon and title in colors.destructive
- Close the sheet after any action: sheetRef.current?.close()

2. Update src/screens/notes/NoteCard.tsx:

- Add onLongPress prop: (() => void) | undefined
- Wrap the outer Pressable with an additional long-press handler:
  - onPress keeps existing behavior (navigate to viewer)
  - onLongPress calls the new onLongPress prop
- Use React Native Pressable's built-in onLongPress prop (delayLongPress: 300ms)

3. Update src/screens/notes/NotesListScreen.tsx:

- Import QuickActionsSheet, BottomSheetModalProvider
- Add state: selectedNote: NoteWithCourse | null = null
- Add BottomSheetModal ref: bottomSheetRef = useRef(BottomSheet)
- Add handleQuickAction handler: sets selectedNote and opens bottom sheet
  - setSelectedNote(note); bottomSheetRef.current?.present()
- Wrap the NotesListScreen return JSX with BottomSheetModalProvider
- Pass onLongPress={handleQuickAction} to each NoteCard
- Render QuickActionsSheet at bottom of JSX:
  - note={selectedNote}, sheetRef={bottomSheetRef}
- After deleteNote completes: setSelectedNote(null), refetch notes
  </action>

<verify>
<automated>npx tsc --noEmit --pretty 2>&1 | grep -E "(QuickActionsSheet|NoteCard|NotesList)" | head -20; echo "EXIT:$?"</automated>
</verify>

<done>
- QuickActionsSheet renders 5 action rows: View, Summary, Flashcards, Quiz, Delete
- Long-pressing a note card opens the quick actions bottom sheet
- View action navigates to NoteViewerScreen
- Summary action navigates to NoteViewer (AI generation in Plan 04)
- Flashcards and Quiz actions show "coming soon" alerts
- Delete action shows confirmation dialog before removing note
- Bottom sheet uses @gorhom/bottom-sheet with dark theme styling
- NoteCard supports both onPress (view) and onLongPress (quick actions)
- Delete confirmation has Cancel and Delete buttons
- TypeScript compiles without errors related to these files
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                        | Description                                                   |
| ------------------------------- | ------------------------------------------------------------- |
| Quick actions sheet to Supabase | Delete action sends authenticated request to Supabase         |
| HTML content rendering          | User-generated HTML rendered in WebView/RichEditor - XSS risk |

## STRIDE Threat Register

| Threat ID | Category      | Component                 | Disposition | Mitigation Plan                                                                                            |
| --------- | ------------- | ------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------- |
| T-03-10   | X (XSS)       | NoteViewer HTML rendering | mitigate    | Sanitize HTML before display - strip script tags and on\* event handler attributes using regex replacement |
| T-03-11   | I (Tampering) | Delete note action        | mitigate    | Confirm deletion with Alert dialog before calling deleteNote - prevents accidental deletion                |
| T-03-12   | E (Elevation) | Quick actions sheet       | accept      | Actions are user-initiated and only affect the user's own notes - no cross-user impact                     |

</threat_model>

<verification>
1. Run TypeScript type check: npx tsc --noEmit - zero errors in new files
2. Verify NoteViewerScreen exists: grep -c 'NoteViewerScreen' src/screens/notes/NoteViewerScreen.tsx returns at least 1
3. Verify HTML sanitization: grep -c 'script\|on\\w+' src/screens/notes/NoteViewerScreen.tsx returns at least 1
4. Verify QuickActionsSheet uses bottom-sheet: grep -c 'BottomSheet' src/screens/notes/QuickActionsSheet.tsx returns at least 2
5. Verify NoteCard has onLongPress: grep -c 'onLongPress' src/screens/notes/NoteCard.tsx returns at least 1
6. Verify delete confirmation: grep -c 'Delete Note' src/screens/notes/QuickActionsSheet.tsx returns at least 1
</verification>

<success_criteria>

- NoteViewerScreen displays full note content with HTML rendering and dark theme
- Note metadata (source badge, course tag, date) shown in viewer
- Edit button in viewer navigates to NoteEditor
- AI action FAB appears at bottom-right of viewer
- Quick actions bottom sheet opens on long-press of note card
- Quick actions include: View, Generate Summary, Generate Flashcards, Generate Quiz, Delete
- Delete action requires confirmation before removing note
- Flashcard and Quiz actions show "coming soon" alerts
- HTML content is sanitized before rendering
- All TypeScript compiles without errors
  </success_criteria>

<output>
After completion, create .planning/phases/03-smart-notes/03-03-SUMMARY.md
</output>
